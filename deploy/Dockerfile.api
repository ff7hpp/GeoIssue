FROM node:22-alpine

WORKDIR /app/server
COPY server/package.json server/package-lock.json ./
RUN npm ci --omit=dev
COPY server/src ./src

ENV NODE_ENV=production
EXPOSE 4000
CMD ["sh", "-c", "node src/db/migrations.js && node src/server.js"]
